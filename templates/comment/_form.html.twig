{{ form_start(form, {'attr': {'class': 'w-100'}}) }}
<div class="input-group-prepend d-flex">
	{{ form_widget(form.comment) }}
	<button class="btn btn-primary mx-2 d-flex"><i class="bi bi-send me-1"></i>{{ button_label|default('Envoyer') }}</button>
</div>
{{ form_end(form) }}

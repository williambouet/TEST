{% for category in categories %}
	<li>
		<a class="dropdown-item d-flex align-items-center" href="{{ path('app_inspiration_category', { 'id': category.id }) }}"><i class="bi bi-tag me-3"></i>{{ category.name }}</a>
	</li>
{% endfor %}
